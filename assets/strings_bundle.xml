<?xml version="1.0" encoding="UTF-8"?>
<RootBundle>
    <!-- This section contains configuration information that can be changed by the user. -->
    <Public>
        <!--
        Can be platform default, but by default for this file is "en".

        Supported:
            en - English
            zh - Chinese (Simplified)
        -->
        <Locale code="zh" />
        <!--
        Supports the following values (case insensitive):
            tray - Starts up in the tray, GUI hidden
            gui  - Starts up with the GUI and the tray

        Defaults to gui
        -->
        <InitialWindowState>
            gui
        </InitialWindowState>
        <!--
        Whether on launch, the window should request focus:
            true
            false

        Defaults to false
        -->
        <InitialFocused>
            true
        </InitialFocused>
        <!--
        Whether when clicking the close button, the app should minimize to tray,
            true
            false

        Defaults to true
        -->
        <MinimizeToTray>
            true
        </MinimizeToTray>
        <!--
        Whether certain buttons should try to be just icon (dense) or icons with text (cozy)
            true
            false

        Defaults to false
        -->
        <PreferDenseActionables>
            false
        </PreferDenseActionables>
    </Public>
    <!-- This section contains program constants that the user (you) really don't need to touch.-->
    <Shared>
    </Shared>
</RootBundle>